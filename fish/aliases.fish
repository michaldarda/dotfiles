alias ll 'ls -ltra'

alias vi vim

alias gst 'git status'
alias gch 'git checkout'
alias ga 'git add'
alias gca 'git commit -a -m'
alias gc 'git commit -m'
alias gd 'git diff'
alias gds 'git diff --staged'

alias rl '. ~/.config/fish/config.fish'
